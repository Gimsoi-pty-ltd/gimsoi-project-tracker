---
name: "CodeQL"

"on":
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          # Explicitly scan javascript only.
          # Skipping 'actions' to avoid the "no source code" error.
          languages: javascript
          # Exclude js/missing-token-validation because we use csrf-csrf (Double Submit Cookie).
          config: |
            query-filters:
              - exclude:
                  id: js/missing-token-validation

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
