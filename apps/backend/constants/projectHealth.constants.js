export const PROJECT_HEALTH_STATUS = Object.freeze({
  HEALTHY: "HEALTHY",
  AT_RISK: "AT_RISK",
  DELAYED: "DELAYED",
});

export const PROJECT_HEALTH_THRESHOLDS = Object.freeze({
  HEALTHY_MIN_SCORE: 75,
  AT_RISK_MIN_SCORE: 50,
});
